---
layout: base
title: Latest News
---

<div class="max-w-4xl mx-auto">
  <div class="bg-gradient-to-br from-blue-500 to-purple-600 text-white rounded-lg p-8 mb-8 shadow-lg">
    <h1 class="text-4xl font-bold mb-4">Latest News</h1>
    <p class="text-lg text-blue-100">
      Stay up to date with our latest updates, announcements, and insights.
    </p>
  </div>

  <div class="grid gap-8">
    {%- for post in collections.news | reverse -%}
      <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300">
        <div class="p-6">
          <div class="flex items-center text-sm text-gray-500 mb-4">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
            <time datetime="{{ post.date | dateIso }}">{{ post.date | dateDisplay }}</time>
          </div>
          <h2 class="text-2xl font-bold text-gray-900 mb-4">
            <a href="{{ post.url }}" class="hover:text-blue-600 transition-colors duration-200">{{ post.data.title }}</a>
          </h2>
          {% if post.data.description %}
            <p class="text-gray-600 mb-4">{{ post.data.description }}</p>
          {% endif %}
          <a href="{{ post.url }}" class="inline-flex items-center text-blue-600 hover:text-blue-700">
            Read more
            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </a>
        </div>
      </article>
    {%- endfor -%}
  </div>
</div> 